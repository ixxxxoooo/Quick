# ============================================================================
# Quick — 版本控制忽略规则
#
# 原则：只忽略「可再生成的产物」和「本机私有状态」。
# 凡是描述项目本身的内容（源码、project.yml、Quick.xcodeproj、规则文档）
# 一律提交，绝不忽略。
# ============================================================================

# ---------------------------------------------------------------------------
# macOS 系统文件
# ---------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.fseventsd
Icon?

# ---------------------------------------------------------------------------
# Xcode
#
# 注意：Quick.xcodeproj 由 project.yml 通过 XcodeGen 生成，但它是「提交」的
# （与 Tinycast 一致）——打开工程不该依赖本机是否装了 xcodegen。
# 这里只忽略真正的本机私有状态。
# ---------------------------------------------------------------------------
build/
DerivedData/
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM
*.dSYM.zip
*.momo

# ---------------------------------------------------------------------------
# SwiftPM
#
# 每个 Packages/* 都是独立包，各自有自己的 .build/。
# Package.resolved 不忽略：它是依赖锁定，属于「描述项目」的内容。
# ---------------------------------------------------------------------------
.build/
.swiftpm/

# ---------------------------------------------------------------------------
# 构建 / 测试输出
# ---------------------------------------------------------------------------
*.log
/tmp-*
*.xcresult

# ---------------------------------------------------------------------------
# 本机工具状态（非项目内容）
#
# .specstory/ 是编辑器插件的会话历史，属于个人本机数据，不随项目分发。
# ---------------------------------------------------------------------------
.specstory/
